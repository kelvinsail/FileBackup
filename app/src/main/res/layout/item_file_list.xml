<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:background="?attr/item_click_background_transparent"
              android:orientation="horizontal">

    <ImageView
        android:id="@+id/iv_item_file_list_preview"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="@dimen/base_padding_normal"
        android:scaleType="fitCenter"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/tv_item_file_list"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_vertical|left"
        android:padding="16dp"
        tools:text="download"/>

    <CheckBox
        android:id="@+id/cb_item_file_list"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:checkMark="?android:attr/listChoiceIndicatorMultiple"
        android:gravity="center_vertical|left"
        android:minWidth="0dp"
        android:paddingLeft="@dimen/base_padding_normal"
        android:paddingRight="@dimen/base_padding_normal"
        android:text=""/>

</LinearLayout>
